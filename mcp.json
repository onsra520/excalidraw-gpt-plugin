{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/mcp.schema.json",
  "mcpServers": {
    "excalidraw": {
      "type": "stdio",
      "command": "node",
      "args": ["${PLUGIN_ROOT}/dist/index.js"],
      "cwd": "${PLUGIN_ROOT}"
    }
  }
}
